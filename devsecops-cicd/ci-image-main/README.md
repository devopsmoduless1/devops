# ci-image

## Description
 This Repository contains Docker files to create a CICD Golden Image to be used for CICD Builds </br>
 This has a gitlab cicd file .gitlab-ci.yml where DIND (Docker In docker) is used to create Docker images and stored in </b>
 gitlab registry